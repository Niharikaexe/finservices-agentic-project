"""fsa_evals — golden datasets, metrics and the CI release gate (ARCHITECTURE.md §14).

The gate matters more than the metrics: it runs on every PR and posts a diff table
against `main`. A gate you can merge past is not a gate. Lands in M6.
"""
