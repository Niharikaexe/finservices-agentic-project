# services/ingestion

Policy docs -> LlamaIndex parse -> chunk -> embed -> policy_chunks (pgvector). M1.

Not yet a uv workspace member — see `docs/adr/0003-services-outside-the-root-venv.md`.
It joins the workspace when it gets real code.
